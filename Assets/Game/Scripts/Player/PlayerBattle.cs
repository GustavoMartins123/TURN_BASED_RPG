using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace RPG.GAME
{
    public class PlayerBattle : CharacterBase
    {
        // Start is called before the first frame update
        

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
